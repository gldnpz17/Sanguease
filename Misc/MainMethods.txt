List<BloodDonationDto> GetBloodDonationEvents()
	//get events from the DB
	//return
BloodDonationEventDto GetBloodDonationEventById(int Id)
	//get events from the DB based on Id
	//return
void AddBloodDonationEvent(BloodDonationEventOptions options)
	//add it into the DB
	//update stats
void RemoveBloodDonationEvent(int Id)
	//remove from the DB
void ConfirmAttendance(string email, int bloodDonationEventId)
	//get the user w/ the matching email
	//add the event to the user
	//update stats

void ExchangePoints(string authId, int itemId)
	//remove points from the user
	//add item to the user
	//update stats
void ConsumeItem(string authId, int itemId)
	//copy item to the ConsumedItem table
	//delete the user's copy of the item
	//update stats
List<InventoryItemDto> GetConsumedItems()
	//get the user's list of consumed items from the DB
	//return
dddddddddddddddd
List<AchievementItemDto> GetAchievements()
	//get the list of achievements form the DB
	//return DB
List<AchievementItemDto> GetOwnedAchievements()
	//get the list of owned achievements from the DB
	//return
AchievementItemDto GetAchievementItemById(int Id)
	//retrieve from the DB
	//return	

void AssignStaff(int staffId, int bloodDonationEventId, DateTime startDate, DateTime endDate)
	//store to DB
void GetAssignment(string authId)
	//retrieve data from the DB
	//return